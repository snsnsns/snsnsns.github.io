name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v2
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2.2' # Specify a valid Ruby version here
      - name: Install dependencies
        run: bundle install
      # other steps here


    - name: Set up Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: 3.2.2 # Specify your Ruby version
        jobs:


    - name: Remove Gemfile.lock
      run: rm -f Gemfile.lock

    - name: Install dependencies
      run: bundle install

    - name: Run tests
      run: bundle exec rake test # Replace with your test command
